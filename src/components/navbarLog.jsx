import { Link } from 'react-router-dom';
import NavBarTo from './navBarTo';

function NavbarLog() {
    return (
        <nav>
            <NavBarTo link={"/"} />
        </nav>
    )
}   

export default NavbarLog;